# Agent Instructions

- Do not create Git commits unless the user explicitly asks for a commit.
- Do not push changes unless the user explicitly asks for a push or PR.
- Keep code changes focused on the user's current request.
- Do not start or offer preview/dev servers for verification; the user keeps the server running.
